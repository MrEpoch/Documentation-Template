---
title: Animovaný wrapper
description: Wrapper obsahující animace od framer motion
---

## Příprava

### Instalace Framer motion

```bash
 pnpm i framer-motion
```

## Kód animované komponenty

### Typescript

```typescript
interface AnimatedDivProps {
  className?: string;
  children: React.ReactNode;
  animationLength?: number;
  side?: "top" | "bottom" | "left" | "right";
}
```

### Komponent tranzise

```tsx
"use client";
import { useInView } from "framer-motion";
import { useRef } from "react";
import { AnimatedDivProps } from "@/types";

export function AnimatedDiv({
  className,
  children,
  animationLength = 0.5,
  side = "bottom",
}: AnimatedDivProps) {
  const ref = useRef(null);
  const isInView = useInView(ref, { once: true });

  function setSide(sideM: "top" | "bottom" | "left" | "right") {
    switch (sideM) {
      case "top":
        return "translateY(-20px)";
      case "bottom":
        return "translateY(20px)";
      case "left":
        return "translateX(-20px)";
      case "right":
        return "translateX(20px)";
    }
  }

  return (
    <div
      className={className}
      ref={ref}
      style={{
        opacity: isInView ? 1 : 0,
        transform: isInView ? "none" : setSide(side),
        transition: `all ${animationLength}s cubic-bezier(0.17, 0.55, 0.55, 1)`,
      }}
    >
      {children}
    </div>
  );
}
```

## Reference na dokumentaci

1. [Framer motion](https://www.framer.com/motion/introduction/)
